/* Minimal, robust, RTL-friendly styling */
:root{
  --bg1:#06101f;
  --bg2:#0a1e34;
  --card:#0b223a;
  --card2:#0a1b2f;
  --line:rgba(255,255,255,.10);
  --text:#e8f1ff;
  --muted:#b8c7dd;
  --accent:#4aa3ff;
}
*{box-sizing:border-box}
html,body{height:100%}
body{
  margin:0;
  font-family: system-ui, -apple-system, "Segoe UI", Tahoma, Arial, sans-serif;
  background: radial-gradient(900px 500px at 75% 15%, rgba(74,163,255,.22), transparent 55%),
              radial-gradient(900px 500px at 20% 70%, rgba(74,163,255,.12), transparent 60%),
              linear-gradient(180deg,var(--bg1),var(--bg2));
  color:var(--text);
}
a{color:inherit;text-decoration:none}
code{background:rgba(255,255,255,.08); padding:.12rem .35rem; border-radius:.35rem}
.container{width:min(1100px, 92vw); margin:22px auto 34px}
.topbar{
  position:sticky; top:0; z-index:10;
  backdrop-filter: blur(10px);
  background: rgba(5,14,26,.72);
  border-bottom:1px solid var(--line);
  padding:12px 16px;
  display:flex; align-items:center; justify-content:space-between; gap:14px;
}

.topbar-logos{
  display:flex;
  align-items:center;
  gap:10px;
  flex:0 0 auto;
}

.logo{
  width:44px;
  height:44px;
  object-fit:contain;
  filter: drop-shadow(0 6px 16px rgba(0,0,0,.45));
  opacity:.95;
}

.hero{ position:relative; }
.hero-bg{
  position:absolute;
  inset:0;
  width:100%;
  height:100%;
  object-fit:cover;
  opacity:.55;
  transform: scale(1.02);
}
.hero-logo{
  position:absolute;
  top:18px;
  width:84px;
  height:84px;
  object-fit:contain;
  filter: drop-shadow(0 8px 22px rgba(0,0,0,.55));
  opacity:.98;
  z-index:2;
}
.hero-logo-left{ left:18px; }
.hero-logo-right{ right:18px; }

.hero-card{ position:relative; z-index:3; }

@media (max-width: 720px){
  .logo{ width:34px; height:34px; }
  .hero-logo{ width:60px; height:60px; top:12px; }
  .hero-logo-left{ left:12px; }
  .hero-logo-right{ right:12px; }
}
.brand-title{font-weight:800; letter-spacing:.2px}
.brand-sub{font-size:12px; color:var(--muted); margin-top:2px}
.nav{display:flex; gap:10px; flex-wrap:wrap}
.nav-link{
  padding:8px 12px; border:1px solid var(--line); border-radius:999px;
  color:var(--muted); font-weight:700; font-size:13px;
}
.nav-link.active, .nav-link:hover{color:var(--text); border-color:rgba(74,163,255,.45); background:rgba(74,163,255,.10)}
.hero{margin-top:10px}
.hero-card{
  background: linear-gradient(180deg, rgba(10,32,55,.88), rgba(8,24,40,.88));
  border:1px solid var(--line);
  border-radius:18px;
  padding:22px;
}
.hero-card h1{margin:0 0 10px; font-size:34px}
.btn-row{display:flex; gap:10px; flex-wrap:wrap; margin-top:14px}
.btn{
  background:rgba(74,163,255,.14);
  border:1px solid rgba(74,163,255,.35);
  padding:10px 14px;
  border-radius:12px;
  font-weight:800;
}
.btn:hover{background:rgba(74,163,255,.22)}
.card{
  margin-top:16px;
  background: linear-gradient(180deg, rgba(11,34,58,.92), rgba(10,27,47,.92));
  border:1px solid var(--line);
  border-radius:18px;
  overflow:hidden;
}
.card-h{
  padding:14px 16px;
  border-bottom:1px solid var(--line);
  display:flex; align-items:center; justify-content:space-between; gap:12px;
}
.card-h h2{margin:0; font-size:18px}
.card-b{padding:14px 16px}
.badge{
  min-width:34px; text-align:center;
  padding:6px 10px;
  border-radius:999px;
  border:1px solid rgba(255,255,255,.12);
  color:var(--muted);
  font-weight:900; font-size:12px;
}
.table-wrap{overflow:auto}
.table{
  width:100%;
  border-collapse:collapse;
  min-width:780px;
}
.table th,.table td{
  border-bottom:1px solid var(--line);
  padding:10px 10px;
  text-align:right;
  white-space:nowrap;
  font-size:14px;
}
.table th{color:var(--muted); font-weight:900; font-size:13px}
.muted{color:var(--muted)}
.small{font-size:12px}
.note{
  margin-top:10px;
  padding:10px 12px;
  border-radius:12px;
  border:1px solid rgba(255,80,80,.35);
  background: rgba(255,80,80,.10);
  color:#ffd7d7;
}
.hidden{display:none}
.round{
  border:1px solid var(--line);
  border-radius:14px;
  padding:12px;
  margin-bottom:12px;
}
.round-title{font-weight:900; margin:0 0 10px}
.match-row{
  display:grid;
  grid-template-columns: 80px 110px 1fr 110px 1fr 90px;
  gap:8px;
  align-items:center;
  padding:10px;
  border-radius:12px;
  border:1px solid rgba(255,255,255,.08);
  background: rgba(255,255,255,.04);
  margin-bottom:8px;
}
.match-row .score{font-weight:900; text-align:center}
.match-row .pill{
  justify-self:start;
  padding:6px 10px;
  border-radius:999px;
  border:1px solid rgba(74,163,255,.35);
  background: rgba(74,163,255,.10);
  font-weight:900;
  font-size:12px;
}
.match-row .dim{color:var(--muted); font-size:12px}
.match-row a{color:var(--accent); font-weight:900}
.grid2{
  display:grid;
  grid-template-columns: repeat(2, minmax(0,1fr));
  gap:12px;
}
.kv{border:1px solid rgba(255,255,255,.08); background:rgba(255,255,255,.04); border-radius:14px; padding:12px}
.kv .k{color:var(--muted); font-size:12px; font-weight:900}
.kv .v{margin-top:6px; font-weight:800}
.sep{border:none; border-top:1px solid var(--line); margin:16px 0}
.scoreline{
  font-size:22px;
  font-weight:1000;
  text-align:center;
  padding:10px;
  border-radius:14px;
  border:1px solid rgba(255,255,255,.08);
  background:rgba(255,255,255,.04);
}
.goals{display:grid; grid-template-columns: repeat(2, minmax(0,1fr)); gap:12px; margin-top:12px}
.goals-col{border:1px solid rgba(255,255,255,.08); background:rgba(255,255,255,.04); border-radius:14px; padding:12px}
.goals-col h3{margin:0 0 8px; font-size:14px}
.goals-col ul{margin:0; padding:0 18px}
.footer{
  border-top:1px solid var(--line);
  color:var(--muted);
  padding:14px 16px;
  text-align:center;
}
@media (max-width: 820px){
  .hero-card h1{font-size:26px}
  .grid2{grid-template-columns:1fr}
  .match-row{grid-template-columns: 70px 92px 1fr 90px 1fr 80px}
}


/* ===== Mobile-first refinements (phone friendly) ===== */
@media (max-width: 760px){
  .container{padding:12px;}
  .topbar{padding:10px 12px; gap:10px; flex-wrap:wrap;}
  .topbar-logos{order:1; gap:8px;}
  .topbar-logos .logo{width:34px; height:auto;}
  .brand{order:2; width:calc(100% - 100px);}
  .brand-title{font-size:16px; line-height:1.25;}
  .brand-sub{font-size:12px; opacity:.9;}
  .nav{order:3; width:100%; overflow-x:auto; white-space:nowrap; -webkit-overflow-scrolling:touch; justify-content:flex-start; padding:6px 0; gap:6px;}
  .nav::-webkit-scrollbar{height:6px;}
  .nav-link{display:inline-block; padding:8px 10px; font-size:13px; border-radius:999px;}
  .hero{padding:14px;}
  .hero h1{font-size:28px;}
  .hero p{font-size:14px; line-height:1.7;}
  .kpis{grid-template-columns:repeat(2,1fr); gap:10px;}
  .kpi{padding:10px;}
  .kpi b{font-size:22px;}
  .card{border-radius:16px;}
  .card-h{padding:10px 12px;}
  .card-b{padding:10px 12px;}
  .table-wrap{overflow-x:auto; -webkit-overflow-scrolling:touch;}
  table{min-width:720px;}
  th,td{font-size:13px; padding:10px 10px;}
  .btn-row{flex-direction:column; gap:10px;}
  .btn{width:100%; justify-content:center;}
  .footer{padding:14px 12px; font-size:12px;}
}

/* Extra small phones */
@media (max-width: 420px){
  .brand{width:100%;}
  .brand-title{font-size:15px;}
  .hero h1{font-size:24px;}
  table{min-width:680px;}
}


/* ===== Mobile fixes (group details, header, logos) ===== */
@media (max-width: 720px){
  header.topbar{
    flex-wrap: wrap;
    gap: 10px;
    padding: 10px 12px;
  }
  header.topbar .topbar-logos{
    flex: 0 0 auto;
  }
  header.topbar img.logo{
    height: 34px;
    width: auto;
    opacity: 1;
    filter: none;
  }
  header.topbar .brand{
    flex: 1 1 100%;
    order: 2;
    text-align: center;
    margin: 0;
  }
  header.topbar .brand-title,
  header.topbar .brand-sub{
    text-align: center;
    width: 100%;
  }
  header.topbar .nav{
    order: 3;
    width: 100%;
    justify-content: center;
    overflow-x: auto;
    -webkit-overflow-scrolling: touch;
    padding-bottom: 6px;
  }
  header.topbar .nav::-webkit-scrollbar{height:6px}
  header.topbar .nav-link{white-space: nowrap}
}

/* Make 'تفاصيل' always visible on mobile in recent matches table */
@media (max-width: 720px){
  .table-wrap{overflow-x:auto;-webkit-overflow-scrolling:touch}
  .table{min-width: 760px;}
  .table td:last-child a,
  .table td:last-child .link{
    display:inline-block;
    padding:8px 12px;
    border-radius: 12px;
    background: rgba(26,120,255,.22);
    border: 1px solid rgba(26,120,255,.35);
    color: #eaf3ff;
    font-weight: 800;
  }
}

/* Group page: match rows become stacked cards so the details button doesn't disappear */
@media (max-width: 640px){
  .match-row{
    grid-template-columns: 1fr;
    gap: 10px;
    padding: 12px;
  }
  .match-row > div:nth-child(1){
    justify-self: center;
    margin-bottom: 2px;
  }
  .match-row > div:nth-child(2){
    display: none; /* hide code inside row (already shown on badge) */
  }
  .match-row > div:nth-child(3),
  .match-row > div:nth-child(5){
    text-align: center;
    font-weight: 800;
    font-size: 16px;
    line-height: 1.5;
  }
  .match-row > div:nth-child(4){
    text-align: center;
    font-size: 26px;
    font-weight: 900;
    letter-spacing: .5px;
    padding: 6px 0;
  }
  .match-row > div:nth-child(6){
    text-align: center;
  }
  .match-row > div:nth-child(6) a,
  .match-row > div:nth-child(6) .link{
    display: block;
    width: 100%;
    padding: 12px 14px;
    border-radius: 16px;
    background: rgba(26,120,255,.20);
    border: 1px solid rgba(26,120,255,.35);
    color: #eaf3ff;
    font-weight: 900;
  }
  .match-row > div:nth-child(7){
    margin-top: 2px;
    font-size: 13px;
    line-height: 1.9;
    opacity: .95;
  }
}

/* Hero logos on mobile: keep them visible */
@media (max-width: 640px){
  .hero-logo{
    opacity: 1;
    filter: none;
  }
  .hero-logo-left{left: 10px;}
  .hero-logo-right{right: 10px;}
  .hero-card h1{ text-align:center;}
  .hero-card .muted{ text-align:center;}
}


/* ===== Group color accents ===== */
:root{
  --ga:#ffffff;         /* Group A: white */
  --gb:#d6b35f;         /* Group B: gold */
  --gc:#0b0f16;         /* Group C: dark black */
  --gd:#6d1a1a;         /* Group D: dark red */
}

/* Hero centered tournament logo */
.hero-logo-center{
  left:50%;
  transform: translateX(-50%);
  width:110px;
  height:110px;
  top:10px;
  background: transparent;
}

/* Center nav and keep mobile layout */
.topbar{
  flex-wrap:wrap;
  justify-content:center;
  text-align:center;
}
.topbar .brand{flex: 1 1 280px; order:1; text-align:center}
.topbar .nav{flex: 1 1 100%; order:2; justify-content:center}
.topbar .topbar-logos{order:0}

@media (max-width: 720px){
  .hero-logo-center{ width:78px; height:78px; top:8px; }
  .topbar{gap:10px}
}

/* Group buttons */
.btn-ga{ border-color: rgba(255,255,255,.45); background: rgba(255,255,255,.10); }
.btn-gb{ border-color: rgba(214,179,95,.55); background: rgba(214,179,95,.10); }
.btn-gc{ border-color: rgba(0,0,0,.35); background: rgba(0,0,0,.18); }
.btn-gd{ border-color: rgba(109,26,26,.55); background: rgba(109,26,26,.16); }

/* Group page accent bar */
body[data-group="A"] .card-h{ border-bottom-color: rgba(255,255,255,.25); }
body[data-group="B"] .card-h{ border-bottom-color: rgba(214,179,95,.35); }
body[data-group="C"] .card-h{ border-bottom-color: rgba(0,0,0,.30); }
body[data-group="D"] .card-h{ border-bottom-color: rgba(109,26,26,.35); }

/* Remove old technical note blocks on hero */
.hero-desc{ font-size:15px; line-height:1.8; }


/* Awards */
.aw-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:14px;margin-top:12px}
@media (max-width: 900px){.aw-grid{grid-template-columns:1fr}}
.award{background:rgba(255,255,255,0.03);border:1px solid rgba(255,255,255,0.06);border-radius:18px;padding:16px}
.award-h{font-size:14px;opacity:.9;margin-bottom:8px}
.award-m{font-size:22px;font-weight:800;letter-spacing:.2px}
.award-s{margin-top:6px;font-size:13px;opacity:.8}
.muted{opacity:.75;font-size:13px}


/* --- Added pages: groups_all + awards --- */
.grid-2{
  display:grid;
  grid-template-columns: 1fr 1fr;
  gap:16px;
}
@media (max-width: 900px){
  .grid-2{ grid-template-columns:1fr; }
}
.page-head{ margin: 18px 0; display:flex; align-items:center; justify-content:space-between; gap:12px; flex-wrap:wrap; }
.page-title{ margin:0; font-size:22px; }
.alert{
  padding:10px 12px;
  border-radius:12px;
  border:1px solid rgba(255,255,255,.12);
  background: rgba(180, 30, 30, .22);
  color:#fff;
  font-size:14px;
}
.hidden{ display:none !important; }
.hint{
  margin-top:16px;
  font-size:13px;
  opacity:.85;
}
.award-card .award-head{
  font-size:18px;
  font-weight:700;
  margin-bottom:10px;
}
.award-card .award-body{
  padding:6px 0 2px 0;
}
.award-card .award-name{
  font-size:22px;
  font-weight:800;
}
.award-card .award-sub{
  margin-top:6px;
  font-size:14px;
  opacity:.9;
}
